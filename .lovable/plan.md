
# إزالة الفراغات — تعديل بسيط ومباشر

**ملف واحد**: `src/components/landing/LandingPage.tsx`

## التغييرات:
- تقليل padding الهيرو (`py-2` بدل `py-4 lg:py-6`)
- تصغير حجم الهاتف أكثر (`scale-[0.45]` بدل `scale-[0.55]`)
- إزالة `mt-4 lg:mt-6` من الشات

**النتيجة**: على 988x592، يظهر الهيرو + عنوان المدونة + التصنيفات + بداية البطاقات بدون تمرير.
